fn find_averages_of_subarrays(k: i32, arr: Vec<i32>) {
    let result: Vec<f32> = Vec::new();
    let _len = arr.len() - k + 1;
    for i in 0.._len {
        let mut _sum: f32 = 0.0;
        for j in kk
    }
}


fn main() {
    println!("Hello, world");
}
